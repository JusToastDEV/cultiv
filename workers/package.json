{
  "name": "sealed-heavens-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute sealed-heavens-db --file=schema.sql",
    "db:migrate:local": "wrangler d1 execute sealed-heavens-db --local --file=schema.sql"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
